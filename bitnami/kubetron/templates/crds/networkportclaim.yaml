{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0

GENERATED by kubetron's hack/gen-chart-crds.sh from its config/crd — DO NOT EDIT.
Editing this by hand is how spec.overflowPolicy and spec.subnet went missing:
the API server prunes undeclared fields silently, so the feature just stops
working. Change the Go types instead and run `make manifests`.
*/}}

{{- if and .Values.sharedResources.create .Values.crds.install }}
apiVersion: {{ include "common.capabilities.crd.apiVersion" . }}
kind: CustomResourceDefinition
metadata:
  name: networkportclaims.kubetron.network.kubevirt.io
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
    {{- if .Values.crds.keep }}
    helm.sh/resource-policy: keep
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
spec:
  group: kubetron.network.kubevirt.io
  names:
    kind: NetworkPortClaim
    listKind: NetworkPortClaimList
    plural: networkportclaims
    shortNames:
    - npc
    singular: networkportclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.network
      name: Network
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.portID
      name: Port-ID
      type: string
    - jsonPath: .status.macAddress
      name: MAC
      type: string
    - jsonPath: .status.ips[0]
      name: IP
      type: string
    - jsonPath: .status.hostID
      name: Node
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkPortClaim owns a Neutron port whose lifecycle is decoupled from any pod
          (PVC/PV analog). Pods reference it by the port-claims annotation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NetworkPortClaimSpec is the tenant-writable desired state. Tenant/project is
              the OpenStack application credential (CredentialSecretRef), not the namespace —
              the namespace is only an organizational grouping.
            properties:
              credentialSecretRef:
                description: |-
                  CredentialSecretRef names a Secret (same namespace) holding the tenant's
                  OpenStack application credential; kubetron uses it to create the port, so
                  the port lands in the credential's project and Neutron RBAC governs access.
                  Keys: authURL, applicationCredentialID, applicationCredentialSecret.
                type: string
              dnsNameservers:
                description: |-
                  DNSNameservers override for the pod's dnsPolicy:None dnsConfig; empty =>
                  status.dns.nameservers. Every kubetron Pod is "full" (single OVN nic off
                  Cilium, §4.3), so CoreDNS is unreachable and a resolver must be supplied.
                items:
                  type: string
                type: array
              fixedIP:
                description: FixedIP optionally pins the port IP; empty => Neutron
                  allocates.
                type: string
              network:
                description: |-
                  Network is the Neutron network (≈VPC) name or id, resolved with the tenant's
                  own credential — so the tenant can only reference networks its OpenStack
                  project may see (access control is Neutron RBAC, not K8s namespace).
                  Optional when PortID is set (the port already pins the network; if both are
                  given they must agree).
                type: string
              portID:
                description: |-
                  PortID adopts an EXISTING Neutron port (pre-created by the tenant) instead
                  of creating one. The port is looked up with the tenant credential, so
                  Neutron RBAC fails closed on foreign ports. kubetron does not tag adopted
                  ports (no device_owner/device_id), so the orphan GC never touches them, and
                  reclaimPolicy Retain (the default) leaves the port exactly as it was.
                type: string
              reclaimPolicy:
                default: Retain
                description: ReclaimPolicy defaults to Retain (PV-like).
                enum:
                - Retain
                - Delete
                type: string
              routes:
                description: Routes are extra static routes (the pod's default route
                  is on the per-subnet NAD).
                items:
                  description: Route is an extra static route.
                  properties:
                    dst:
                      type: string
                    gw:
                      type: string
                  required:
                  - dst
                  - gw
                  type: object
                type: array
              subnet:
                description: |-
                  Subnet optionally pins the port to ONE subnet (name or ID) of Network;
                  empty => Neutron picks among the network's subnets. Creation only —
                  ignored when adopting via PortID. IP capacity is managed by the tenant
                  on the OpenStack side (enlarge the CIDR); kubetron never touches subnets.
                type: string
            required:
            - credentialSecretRef
            type: object
            x-kubernetes-validations:
            - message: either network (create a port) or portID (adopt an existing
                port) must be set
              rule: (has(self.network) && self.network != "") || (has(self.portID)
                && self.portID != "")
            - message: fixedIP cannot be combined with portID (the adopted port already
                owns its IPs)
              rule: '!(has(self.portID) && self.portID != "") || !(has(self.fixedIP)
                && self.fixedIP != "")'
          status:
            description: |-
              NetworkPortClaimStatus is controller-filled; tenants read-only (status subresource).
              The CEL rule encodes the §3.1 holder invariant: one live pod per claim, except
              two during a live-migration double-bind window (migrationTargetPod set).
            properties:
              boundPods:
                description: BoundPods holds current users; during live-migration
                  it may hold two (§6.4).
                items:
                  type: string
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dns:
                description: DNSStatus carries the subnet's DNS servers.
                properties:
                  nameservers:
                    items:
                      type: string
                    type: array
                type: object
              hostID:
                description: HostID is the current binding:host_id (= the node's OVN
                  chassis id, §6.3).
                type: string
              ips:
                description: IPs in CIDR form with the subnet's real prefix (never
                  /32, §8).
                items:
                  type: string
                type: array
              macAddress:
                type: string
              migrationTargetPod:
                description: MigrationTargetPod non-empty means the claim is in a
                  live-migration double-bind window (§6.4).
                type: string
              networkID:
                type: string
              networkMTU:
                description: |-
                  NetworkMTU is the Neutron network's `mtu`, copied verbatim into the generated
                  NAD so ovs-cni sets it on the veth. Neutron owns the value (computed from
                  global_physnet_mtu minus encapsulation, overridable per network in Horizon);
                  kubetron only transports it, because a full pod's static IPAM never receives
                  the DHCP option 26 that delivers it to a VM. 0 = not reported, NAD omits it.
                type: integer
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: ClaimPhase is the high-level lifecycle phase.
                enum:
                - Pending
                - Bound
                - Released
                - Failed
                type: string
              portID:
                type: string
              projectID:
                type: string
              subnet:
                description: SubnetStatus carries the read-back subnet detail for
                  routing/DNS.
                properties:
                  cidr:
                    type: string
                  gatewayIP:
                    type: string
                  id:
                    type: string
                type: object
            type: object
            x-kubernetes-validations:
            - message: a claim may be held by one pod (two only during live migration)
              rule: '!has(self.boundPods) || ((has(self.migrationTargetPod) && self.migrationTargetPod
                != "") ? size(self.boundPods) <= 2 : size(self.boundPods) <= 1)'
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
